import { MostrarRegistrosPipe } from './mostrar-registros.pipe';

describe('MostrarRegistrosPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarRegistrosPipe();
    expect(pipe).toBeTruthy();
  });
});
